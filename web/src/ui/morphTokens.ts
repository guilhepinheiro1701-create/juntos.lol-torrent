/**
 * Motion tokens for the morphing-menu surface (transitions.dev scale): open
 * 250ms / close 150ms on a smooth-out ease. Shared by the surface itself and
 * by contents that stagger in against the same clock.
 */
export const MORPH_EASE: [number, number, number, number] = [0.22, 1, 0.36, 1]
export const OPEN_DURATION = 0.25
export const CLOSE_DURATION = 0.15
